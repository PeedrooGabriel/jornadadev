FUNCTION Main()

    LOCAL nI := 0
    LOCAL nNum := 0

    FOR nI := 1 to 100
        ? STR(nI)
    NEXT

    FOR nI := -50 to 50
        ? STR(nI)
    NEXT

    FOR nI := 80 to 5 STEP -1
        ? STR(nI)
    NEXT

RETURN NIL